const url = "http://localhost:8000";

const api = {
  product: `${url}/product`,
  category: `${url}/category`,
  user: `${url}/user`,
  auth: `${url}/auth`,
  upload: `${url}/upload`,
  voucher: `${url}/voucher`,
  payment: `${url}/payment`,
};

export default api;
