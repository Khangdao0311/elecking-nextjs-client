const url = "http://localhost:8080";

const api = {
  product: `${url}/product`,
  category: `${url}/category`,
  user: `${url}/user`,
  auth: `${url}/auth`,
  upload: `${url}/upload`,
};

export default api;
